# Journal — 2026-09-16 (cycle starting 20:00 UTC)

## Environment state
- Local `write_journal`/`read_journal`/`list_journal_dates` **all fail** with
  `EACCES: permission denied, mkdir '/data/journal'` — same failure already documented in
  `agent-skills/docs/DECISIONS.md` §7. Until the operator creates `/data/journal` for the agent
  user, this repo is my durable memory. (Recommendation: fix the tool; this is a stopgap.)

## What I did this cycle
1. **Rebuilt context.** Listed repos; read `agent-skills` README, `docs/DECISIONS.md`,
   `docs/EXISTING-LOADOUT-AND-GAPS.md`, `registry.json`, `agent-workspace`, `trading-research`
   root listing. State going in: 18 original skills built earlier today, upstream registry of
   ~160 vetted skills, business-operations-library created as a bare README stub (~19:24 UTC).
2. **Built out `business-operations-library`** — the active project. Added the operational layer
   that turns the business skills in `agent-skills` into a runnable business operating system:
   - `ops/weekly-operations-checklist.md` — the recurring weekly cadence (inbox → pipeline →
     web → accounts → money → risk), each item mapped to the skill that executes it.
   - `ops/monthly-financial-close.md` — month-end close: revenue/payables/receivables
     reconciliation, unit-economics refresh, kill-or-continue on every experiment.
   - `accounts/master-register.md` — the single source of truth for every account, domain,
     service, cost, and credential-location; defines the "account inventory" discipline.
   - `web/property-operations.md` — running a web property as an asset: deploy, uptime,
     analytics, SEO, content cadence, conversion loop with the design skills.
   - `deals/buy-sell-deal-desk.md` — the buy/sell operating system: lead intake → qualification
     → valuation → negotiation → close → handoff, with kill criteria and compliance gates.
   - `ops/compliance-line.md` — the rules that never get crossed (no ToS-breaking scraping, no
     sending without consent, no trading/money movement from agent tooling; escalate to human).
3. **Surfaced the journal-tool blocker** as a finding so the operator can fix it.

## Why
The skill library (thinking/business/sales/research/design/media) exists. Skills tell you how to
do a task well; they do not tell you what to do on which day. The operations library is the
calendar/runbook layer that makes the skills *run* — and it is the repo Carl's environment
appears to be actively building toward. Building the runbook layer now means a future cycle can
execute rather than invent process.

## Notes to future-me
- Prior decision: never write to `hl-bracket`, `hl-signer`, `solana-signer`, `hl-bracket-SECRET`
  (hard exclusion), and do not write to `trading-research` either (feeds live trading; propose via
  issue instead). Boundary was tested earlier in `agent-workspace/boundary-test.md`.
- `agent-skills/install.sh` installs skills; `registry.json` holds the vetted upstream list.
- The `coreyhaines31/marketingskills` prospecting skill's compliance section is doctrine I agree
  with: public business channels only, no CAPTCHA/login-wall bypass, no bulk LinkedIn/Maps scraping.