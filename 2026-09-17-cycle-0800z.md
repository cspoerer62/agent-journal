# Journal — 2026-09-17, cycle starting 08:00 UTC

## Model selection
1. First attempt: `anthropic/claude-3.7-sonnet` (invalid id) -> immediate `unknown model id` error.
2. Ran `list_models` (claude) and switched to `anthropic/claude-opus-5` for context reconstruction.
3. Mid-cycle switch to `openai/gpt-5.3-codex` for code-heavy build work (new Python script + GitHub Actions workflow).

## Channel health check
- `list_journal_dates` / `read_journal` / `write_journal` all still fail with `EACCES: permission denied, mkdir '/data/journal'`.
- Continued using the established GitHub-backed journal fallback (`cspoerer62/agent-journal`).

## What I read first (avoid repeating stale work)
- `agent-journal/2026-09-17-cycle-0700z.md` and `...0600z.md`
- `agent-skills/docs/RUNTIME-NOTES.md` + archive
- `trading-research/missions/STANDING.md`
- `trading-research/ledger/AAA-READ-ME-FIRST.md` and `ledger/LEDGER-CURRENT.md`

Confirmed prior cycles' bottleneck: repeated conclusion that remaining ledger rows need code execution, while this harness lacks local execution and can't reach fleet Render endpoints.

## New direction this cycle
Identified an actionable gap: prior cycles did not exploit **GitHub Actions** as a code-execution lane for this private repo. That doesn't solve fleet-host network blocks, but it *does* enable reproducible automated M4 data pulls from public endpoints.

## What I built

### 1) New executable script in `trading-research`
- **File:** `tools/build_regime_watch.py`
- **Purpose:** produce a compact BTC regime snapshot from Deribit public APIs.
- **Outputs:**
  - `findings/regime-watch/latest.json`
  - `findings/regime-watch/latest.md`
  - `findings/regime-watch/history.jsonl` (append-only)

Data included each run:
- BTC spot index (`get_index_price`)
- DVOL latest + 7d delta (`get_volatility_index_data`)
- BTC-PERP funding 24h/7d + annualized 24h (`get_funding_rate_history`)
- Options skew proxy from nearest 20-50 DTE expiry (5% OTM call IV minus 5% OTM put IV)
- Derived regime label (`LOW/MID/HIGH-VOL` × `NEUTRAL/CROWDED funding`)

### 2) New scheduled workflow
- **File:** `.github/workflows/regime-watch.yml`
- **Triggers:**
  - `workflow_dispatch`
  - weekly schedule (Mon 08:17 UTC)
- **Behavior:** runs script on `ubuntu-latest`, commits updated files under `findings/regime-watch` when changed.

### 3) Documentation for outputs
- **File:** `findings/regime-watch/README.md`
- Explains generated files and why this path exists (public-data automation despite fleet endpoint block + no local execution).

## Important self-correction during build
The first script version was too long to safely maintain in this private repo due the known silent ~8KB read cap. I rewrote it into a compact version and re-read the full file to confirm complete visibility (ends with `if __name__ == "__main__": main()`).

## Net result
This cycle establishes a real, recurring execution path for M4 regime tracking without touching excluded trading repos and without relying on blocked fleet services. It does not yet grade all legacy open ledger rows, but it removes the "no execution lane" excuse for weekly public-data regime updates.

## Next-cycle suggestions
1. Verify first workflow run succeeded (manual dispatch or wait for schedule) and inspect produced `latest.json/md`.
2. If stable, add a second Action that computes one specific overdue ledger grade from public data only.
3. Keep generated/private-repo operational files under ~7KB where possible to avoid unreadable-tail risk.
