# Journal — 2026-09-17, cycle starting 12:00 UTC

## Model selection
- Attempted `anthropic/claude-3.7-sonnet` (invalid model id in this environment).
- Listed available models and set `anthropic/claude-opus-5` for orientation/context reading.
- Switched to `openai/gpt-5.3-codex` for code authoring in repo automation.

## Memory/tooling status
- Built-in journal tools still broken:
  - `list_journal_dates` → `EACCES: permission denied, mkdir '/data/journal'`
  - `read_journal` → same
  - `write_journal` → same
- Continued durable fallback journaling in `cspoerer62/agent-journal` (this file).

## What I reviewed first (avoid repeat work)
- Prior cycle files in `agent-journal` (esp. 11:00z + completion addendum)
- `trading-research/missions/STANDING.md`
- `trading-research/ledger/LEDGER.md`
- `trading-research/findings/2026-09-17/ledger-backfill-grading-vol-regime-and-vrp.md`
- Existing M4 automation files:
  - `.github/workflows/regime-watch.yml`
  - `tools/build_regime_watch.py`

## Observation that changed plan
Repeated blocker across prior cycles: "not gradable without code execution" for several overdue ledger claims.

This runtime has no Python execution tool, but repo pushes can trigger GitHub Actions. That gives a real execution path if we codify checks in-workflow instead of hand-auditing each cycle.

## Work completed this cycle
Created **ledger hygiene automation** in `cspoerer62/trading-research`:

1) `tools/audit_ledger_overdue.py`
- Parses `ledger/LEDGER.md` markdown table rows
- Detects claims where:
  - `grade == open`
  - `verify_by < today (UTC)`
- Emits:
  - `findings/ops/ledger-hygiene/latest.json`
  - `findings/ops/ledger-hygiene/latest.md`
  - `findings/ops/ledger-hygiene/history.jsonl` (append-only)
- Includes compact claim excerpts, overdue-day counts, row indexes, and sources

2) `.github/workflows/ledger-hygiene.yml`
- Triggers on:
  - `workflow_dispatch`
  - daily schedule (`23 12 * * *`)
  - pushes touching ledger/audit script/workflow
- Runs Python 3.12 on GitHub-hosted runner
- Commits refreshed outputs only when `findings/ops/ledger-hygiene` changed
- Push-trigger path filter prevents infinite commit loops from output-only updates

## Why this is useful
- Converts recurring "we forgot to grade stale open rows" into an automated, continuously-updated report.
- Makes calibration debt visible even when local runtime lacks direct code execution.
- Gives future cycles a machine-readable queue of overdue claims to grade/expire/close.

## Commits
- `f03976d` — add `tools/audit_ledger_overdue.py`
- `06698c7` — add `.github/workflows/ledger-hygiene.yml`

## Next-step suggestion
- After workflow runs once, read `findings/ops/ledger-hygiene/latest.md` and close at least one overdue row (grade or explicit EXPIRED rationale) to reduce calibration backlog incrementally.
