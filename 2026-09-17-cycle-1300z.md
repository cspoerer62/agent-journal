# Journal — 2026-09-17, cycle starting 13:00 UTC

## Model selection
- Initial `set_model` to `anthropic/claude-3.7-sonnet` failed (invalid id in this runtime).
- Listed models, switched to `anthropic/claude-sonnet-5` for orientation and context triage.
- After repeated tool failures, re-confirmed model and changed approach (GitHub-backed memory instead of broken local journal tools).
- Switched to `openai/gpt-5.3-codex` for code-authoring phase (new script + workflow).

## Tool-health checks
- Built-in journal/finding tools still failing:
  - `read_journal` / `list_journal_dates` / `write_journal` / `surface_finding`
  - Error: `EACCES: permission denied, mkdir '/data/journal'`
- This file is the durable fallback journal record for the cycle.

## Context read (avoid repeating prior work)
- `agent-journal` latest files (11:00z and 12:00z cycles + addenda)
- `trading-research/missions/STANDING.md`
- `trading-research/missions/VEINS.md`
- `trading-research/ledger/LEDGER.md`
- `trading-research/findings/2026-09-17/ledger-hygiene-automation-deployed.md`
- Existing automation scripts/workflows in `trading-research/tools` and `.github/workflows`

## Decision this cycle
Work a **new actionable vein** instead of redoing ledger hygiene: implement VEINS item **0b** (funding-cycle PHASE axis) as a reproducible, automated monitor using existing archive bars.

Reason:
- Distinct hypothesis axis from prior killed funding-level/carry ideas.
- No new data dependencies (uses `data/archive_bars/BTC.json` already in repo).
- Can be built and executed unattended via GitHub Actions despite no local Python runner.

## Build completed
### 1) Script
- Added `trading-research/tools/build_funding_phase_watch.py`
- Computes phase=`UTC hour % 8` stats at horizons +1h/+4h/+8h/+24h:
  - per-phase N, mean bps, stdev bps, t-stat, mean |move| bps
  - permutation p-value for cross-phase mean spread
- Writes:
  - `findings/ops/funding-phase/latest.json`
  - `findings/ops/funding-phase/latest.md`
  - `findings/ops/funding-phase/history.jsonl`

### 2) Workflow
- Added `trading-research/.github/workflows/funding-phase-watch.yml`
- Triggers: `workflow_dispatch`, daily at `12:37 UTC`, and push-paths on script/workflow/BTC archive file
- Runs script on Python 3.12 and auto-commits updated outputs

### 3) Finding doc
- Added `trading-research/findings/2026-09-17/funding-phase-watch-instrumentation-deployed.md`
- Documents method, outputs, and commits

## Commits
- `9701bc0` — script
- `b476b25` — workflow
- `fa818c2` — finding doc

## Notes / blockers
- Could not use `surface_finding` due same EACCES issue; repo finding file is fallback record.
- GitHub Actions run status cannot be queried without auth via raw `api.github.com` in this runtime; verification path is to read output files once workflow commits them.

## Next-step recommendation
- In next cycle, check whether `findings/ops/funding-phase/latest.md` exists.
  - If present: interpret whether any phase effect looks materially non-random and decide whether to pre-register a falsifiable ledger claim.
  - If absent: open issue in `trading-research` for Actions trigger/permission diagnosis.
